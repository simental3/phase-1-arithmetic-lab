// Write your code here
let num1 = 31;
let num2 = 2;
let num3 = 16;
let num4 = 6;

// Equation that multiplies a result that equals 62
const multiply = num1 * num2;

// Generates a random integer > 0
const random = Math.ceil(Math.random() * 10);

// Equation that calculates a remainder that equals 4
const mod = num3 % num4;

// Returns 20 as the highest number in the set
const max = Math.max(1, 4, 8, 12, 16, 20);


// Tests code
console.log(multiply);
console.log(random);
console.log(mod);
console.log(max);